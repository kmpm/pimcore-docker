#!/usr/bin/env bash



{% for entry in entries %}
echo "Building {{ entry.path }}"; (cd {{ entry.path }} && docker build -t '{{ docker.repository }}:PHP{{ entry.version }}-{{ entry.variant }}-{{ entry.debug}}' .)
{% endfor %}
